<Window x:Class="AI_Training.Gui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI Training Academy" Height="450" Width="700"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeaderText" Grid.Row="0" FontSize="16" FontWeight="Bold" Margin="0,0,0,8" />
        <TextBlock x:Name="ErrorText" Grid.Row="1" Foreground="Red" TextWrapping="Wrap" Margin="0,0,0,8" />
        <Button x:Name="RefreshButton" Grid.Row="2" Content="Refresh" Padding="8,4" HorizontalAlignment="Left"
                Margin="0,0,0,8" Click="RefreshButton_Click" />

        <DataGrid x:Name="LevelsGrid" Grid.Row="3" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80" />
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
